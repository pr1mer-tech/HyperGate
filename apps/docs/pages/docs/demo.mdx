---
layout: docs
title: Demo
description: A demo of the HyperGate React components.
---

import { ConnectWallet } from "../../components/connect-wallet";
import { GetBalance } from "../../components/balance";
import { WatchLedger } from "../../components/block";

# Demo

### Connect Wallet

:::code-group

<div data-title="Preview">
  <ConnectWallet />
</div>

```tsx twoslash [example.tsx]
// @allowUmdGlobalAccess
// @module: esnext
// @moduleResolution: bundler

const Button = React.forwardRef<HTMLButtonElement, any>(
  ({ className, variant, size, asChild = false, ...props }, ref) => {
    return <button ref={ref} className={className} {...props} />;
  },
);
Button.displayName = "Button";
const Separator = React.forwardRef<HTMLDivElement, any>(
  ({ className, variant, size, asChild = false, ...props }, ref) => {
    return <div ref={ref} className={className} {...props} />;
  },
);
Separator.displayName = "Separator";

// ---cut---
import { useAccount, useConnect, useConnectors } from "@hyper-gate/react";
import { ConnectKitButton } from "@hyper-gate/connectkit";

// [!include ~/components/connect-wallet.tsx:connect-wallet]
```

:::

### Balance

:::code-group

<div data-title="Preview">
  <GetBalance />
</div>

```tsx twoslash [example.tsx]
// @allowUmdGlobalAccess
// @module: esnext
// @moduleResolution: bundler

const Input = React.forwardRef<HTMLInputElement, any>(
  ({ className, variant, size, asChild = false, ...props }, ref) => {
    return <input ref={ref} className={className} {...props} />;
  },
);
Input.displayName = "Input";
// ---cut---
import { useAccount, useAccountEffect, useBalance } from "@hyper-gate/react";
import { Address, formatHumanUnits } from "@hyper-gate/core";
import { useState } from "react";

// [!include ~/components/balance.tsx:balance]
```

:::

### Block

:::code-group

<div data-title="Preview">
  <WatchLedger />
</div>

```tsx twoslash [example.tsx]
// @allowUmdGlobalAccess
// @module: esnext
// @moduleResolution: bundler
// [!include ~/components/block.tsx]
```

:::
